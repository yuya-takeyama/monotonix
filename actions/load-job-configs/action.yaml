name: 'Load Job Configs'
description: 'Load and filter Job Configs from Local Config Files'
author: 'Yuya Takeyama'

inputs:
  root-dir:
    description: 'Root directory'
    required: true

outputs:
  result:
    description: 'Job Configs'

runs:
  using: 'node20'
  main: 'dist/index.js'
