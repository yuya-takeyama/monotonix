{
  "name": "@monotonix/generate-generic-job-params",
  "scripts": {
    "build": "tsc",
    "ci-test": "jest",
    "format": "prettier --write .",
    "package": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run format && npm run test && npm run package"
  },
  "dependencies": {
    "@monotonix/schema": "*",
    "js-yaml": "^4.1.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "@vercel/ncc": "^0.38.3",
    "jest": "^29.7.0",
    "prettier": "^3.4.2",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.2"
  }
}
